<!-- PR #121 | docs: session wrap-up + multi-model research & next-focus -->

The input you provided is already the contents of `CHANGELOG_PR.md` — essentially a single-PR ChangeDoc. The "consolidate" ask is to tighten it into one clean doc (drop the duplicated PR-header line, deduplicate TL;DR vs body, compress files-touched + follow-ups, normalize formatting).

Here's the consolidated version:

---

# PR #121 — docs: session wrap-up + multi-model research & next focus

## TL;DR
Docs-only. Closes the 2026-06-06 session, confirms **changedoc → ARK** is fully wired (generic `OPENAI_API_KEY`, curl-validated HTTP 200, per #120), and sets the **2026-06-07 next focus: multi-model selection** — promoted from LATER to 🎯 NEXT in `ROADMAP.md` / `STATUS.md`, backed by a new 202-line research + phased plan. MVP scope is locked to a **same-gateway (ARK) model switch**: curated registry + `model` plumbing mirroring `skillSlug` / `permissionTier`, a real composer picker, and `query({ model })` — no DB, no baseURL/token switching. Cross-provider routing, failover, per-capability key split, and capability gating are explicitly deferred to Phase 4.

## Why
The prior session shipped Phase C (Path A on a real Linux VM, preview sharing, changedoc→ARK). With Phase C closed, the owner picked the next focus: turn the cosmetic "GLM 5.0" composer badge into a real per-run model selector. This PR captures the code audit, SDK-imposed constraints, and a phased plan **before** any code lands, so the implementation PR is scoped and reviewable.

## What changed (docs only)

**`docs/project/ROADMAP.md`**
- Adds 🎯 NEXT: **multi-model selection (owner-set 2026-06-07)**, MVP = same-gateway ARK switch, links research file.
- LATER section header: "Multi-model, gates, accounting, polish" → "gates, accounting, polish".
- Rewrites the LATER "Multi-model" bullet to note the NEXT promotion and reserve Phase 4 for cross-provider routing/failover + per-capability key split.

**`docs/project/STATUS.md`**
- "Last updated" → **2026-06-07**.
- `changedoc` row: "to go green: ensure `ARK_API_KEY` visible…" → **wired ✅** (`OPENAI_API_KEY`, #120, curl-validated; next PR confirms green).
- Splits multi-model backlog row into 🎯 **NEXT** (MVP, S–M) and 🟣 **LATER** (Phase 4 stretch).
- Adds **2026-06-07 decision-log entry**: current state, SDK constraint, per-request env-override enabler, MVP shape, v2/Phase 4 outline, LibreChat/Lobe prior-art note.
- Tightens 2026-06-06 changedoc bullet to reflect final secret name + curl-validated state.

**`docs/project/research/2026-06-multi-model-support-research.md`** *(new, 202 lines)*
- §1 **Code audit** — `ANTHROPIC_MODEL` read once at server startup (`ws-server.mjs` ~L107, `ws-query-worker.mjs` ~L24), passed to `query({ options: { model } })` ~L693; composer badge is cosmetic.
- §2 **SDK constraint** — Claude Agent SDK 0.2.112 is Anthropic-protocol only. Two tiers: A) same-gateway many models, B) cross-gateway via per-request env.
- §3 **Key enabler** — `ws-server` already spawns a fresh worker child per request → can override `ANTHROPIC_MODEL` / `ANTHROPIC_BASE_URL` / `ANTHROPIC_AUTH_TOKEN` per request without needing SDK 0.3.x.
- §4 **Architecture** — registry shape; plumbing mirror of `skillSlug`/`permissionTier`; worker env routing; sub-agent aliasing; session persistence; UI picker; credential split.
- §5 **Phases** — MVP / v2 / Phase 4 stretch.
- §6 **MVP change list** — new `src/config/model-registry.ts`; updates to store, ws-adapter, ws-server, worker, composer.
- §7 **Risks** — verify each MVP model on ARK `/api/coding` **Anthropic** path (the `/v3` OpenAI validation does not transfer); alias coupling; mid-session model change; OpenAI-only providers need a shim; structured-outputs flag interaction.
- §8 **Prior art** — LibreChat (spec→preset, per-message model) and Lobe (provider card, runtime resolve). Borrow patterns; don't copy code.

## Breaking changes
None.

## Test plan
- Render `ROADMAP.md`, `STATUS.md`, and the research file on GitHub; confirm cross-references resolve.
- Decision-log date (2026-06-07) matches "Last updated".
- Spot-check research file's code anchors against reality: `ws-server.mjs` ~L107, `ws-query-worker.mjs` ~L24 and ~L693 `query({ options: { model } })`, hardcoded badge in `chat-composer.tsx` ~L468 — these anchor the implementation PR.
- This PR itself exercises the new `OPENAI_API_KEY`-on-`ai-review` wiring, since `pull_request_target` picks up the base-branch config from #120.

## Files touched
| File | Summary |
|---|---|
| `docs/project/ROADMAP.md` | Promote multi-model to 🎯 NEXT with MVP scope + research link; trim LATER header; rewrite LATER bullet as Phase 4 stretch. |
| `docs/project/STATUS.md` | Bump date to 2026-06-07; mark changedoc wired ✅; split multi-model row into NEXT MVP + LATER stretch; add 2026-06-07 decision-log entry. |
| `docs/project/research/2026-06-multi-model-support-research.md` | **New (202 lines).** Audit, SDK constraint, per-request env design, registry, phased plan, MVP change list, risks, prior art. |

## Suggested follow-ups
- **Implementation PR (MVP, mode A — same-gateway ARK)**:
  - `src/config/model-registry.ts` with curated ARK entries + `getModels()` / `resolveModel(id)` + default.
  - `chat-session-store.ts`: `selectedModelId` + setter.
  - `src/claude/adapters/ws-adapter.ts`: add `model?: string` to the `chat` `InboundMessage`.
  - `ws-server.mjs` `handleChat`: accept + validate `model` against registry (clamp unknown → default), thread into `workerEnv.ANTHROPIC_MODEL` instead of the startup constant.
  - `chat-composer.tsx` ~L468: replace cosmetic "GLM 5.0" badge with a registry-driven `<select>` mirroring the permission-tier picker UX; disable while a run is active.
- **Pre-implementation verification**: curl each candidate MVP model against ARK `/api/coding` on the **Anthropic** path (Risk #1).
- **v2**: `model` column on `agent_session` (resume reflects last model); DB-backed registry + admin curation mirroring Skills; per-message model logging for cost attribution (pairs with existing `modelUsage` telemetry).
- **Phase 4 stretch**: cross-provider routing via per-request env override (mode B); failover; per-capability key split; capability gating (e.g., vision-only models).
- **Adjacent cleanup**: revisit `ENABLE_STRUCTURED_OUTPUTS=off` — Phase C is done, so address the StructuredOutput-leak root cause rather than leaving the flag forced off. Ensure the multi-model switch doesn't re-trigger that hook path.

---

Want me to overwrite `CHANGELOG_PR.md` and/or `_final.md` with this consolidated version?
